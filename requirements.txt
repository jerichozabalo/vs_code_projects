flask==3.0.0
transformers>=4.30.0
torch>=2.0.0  # For model inference (use torch with CUDA if GPU)
langchain>=0.1.0
sentence-transformers>=2.0.0
faiss-cpu>=1.7.0
accelerate>=0.20.0  # For faster inference